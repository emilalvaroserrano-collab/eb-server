# Eburon Hub APK builds

The Android debug APK is generated by GitHub Actions from the current `main` source and committed as `releases/Eburon-Hub-debug-apk.zip`.

The ZIP contains `app-debug.apk` for direct sideload testing on Android devices.
